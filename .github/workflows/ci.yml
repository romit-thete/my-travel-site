---

name: My First CI pipeline

on:
  push:
    branches:
      - dev
      - main

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check if index.template.html exists
        run: |
          if [ ! -f index.template.html ]; then
            echo "❌ index.template.html not found!"
            exit 1
          else
            echo "✅ index.template.html exists"
          fi
      
      - name: Basic HTML Check
        run: |
          if ! grep -q "<html>" index.template.html; then
            echo "❌ Missing <html> tag"
            exit 1
          fi

      - name: Check title exists
        run: |
          if ! grep -q "<title>" index.template.html; then
            echo "❌ Missing title"
            exit 1
          fi
